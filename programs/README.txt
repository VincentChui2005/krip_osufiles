All programs:
  There are settings inside the script, right after the imports. Change them if you want or need to

3K_standard.py: 
  As default the 3rd key is binded to mouse left click, by osu! working principles, m1 is blocked when x1 is pressed no matter which comes first
  This is perfectly legal. You're technically still doing "press a button to click a circle"
  
auto_spinner.py:
  For sure this is illegal, as it generates high rpm spinner without your hard work
  Disabling of raw input required, whenever you want to move your cursor
  
bpm_meter:
  This calculates your STREAM bpm, not jump
  Perfect for consistancy training
